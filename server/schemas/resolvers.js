const { Book, User } = require('../models');

const resolvers = {
    Query: {
        getSingleUser: async () => {

        }
    },
    Mutation: {
        createUser: async () => {

        },
        login: async () => {

        },
        saveBook: async () => {

        },
        deleteBook: async () => {

        }
    }
};